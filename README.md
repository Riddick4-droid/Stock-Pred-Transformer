# Stock-Pred-Transformer
Implementation of stock price prediction(time series data) with a transformer architecture, alongside the inclusion of volatility and other key market/stock metrics like the VIX, RSI, etc
